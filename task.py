task_list = {
    "1":["brush teeth","","0"],
    "2":["clean room","","0"],
    "3":["write improv session","2024-01-31","1"]
}

def add_to_task_list(name,due_date,importance):
    id = auto_increment()
    # pack the name, due date and imporance into an array, then assign the autoincrement
    
def auto_increment():
    """pull the highest number from the task list and add a 1 then define the next # for the dictionary
    """
    pass